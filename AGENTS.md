# AGENTS.md — 포인터 (내용 복사 금지)

이 저장소의 모든 규칙·함정 목록·검증 명령의 **정본은 이 폴더의 `CLAUDE.md`**다.

작업 시작 전 `C:\Users\hosic\.claude\CLAUDE.md`와 이 저장소의 `CLAUDE.md`를 전체 읽고 따른다. 코드 변경 후 검증은 `CLAUDE.md`의 "검증 명령" 섹션을 따른다. 커밋 시 로컬 git `pre-commit` 훅이 관련 검증을 다시 실행하고, 실패하면 커밋을 차단한다.

완료 보고는 전역 Gate Check 양식을 따른다: 변경 파일 / 실행한 검증 / 신뢰도(🟢🟡🔴) / 미해결 사항.
