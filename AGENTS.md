# AGENTS.md — 포인터 (내용 복사 금지)

이 저장소의 모든 규칙·함정 목록·검증 명령의 **정본은 이 폴더의 `CLAUDE.md`**다.
파일 이름과 무관하게 **모든 에이전트·IDE**(Claude Code / Cursor / Copilot / Codex / 그 외)에 적용된다.

## 시작할 때

1. 이 저장소의 **`CLAUDE.md` 전체** — 특히 「⚠️ 함정·금지 목록」 22개
2. **`docs/HANDOFF_SESSION.md`** — 지금 어디까지 왔고 다음에 뭘 하면 되는가 (다른 IDE 로 이어받는 경우 여기부터)
3. `git log --oneline -30`

Claude Code 로 여는 경우엔 사용자 전역 규칙(`~/.claude/CLAUDE.md`)도 자동 로드된다.
다른 IDE 라면 그 파일이 없을 수 있으므로, 아래 항목이 전역 규칙의 요지다.

## 어디서든 지키는 것

- **한국어로 응답한다.** 코드 식별자·기술용어는 원문 유지.
- **코드 변경 후 검증은 예외 없다** — `cd backend && pytest` (824건 기준) + `python -m ruff check backend/`.
  실패 수정은 2회까지, 그 이상이면 [실패 지점 / 원인 추정 / 시도한 것]을 정리해 사람에게 넘긴다.
- **완료 보고 4항목 고정**: 변경 파일 / 실행한 검증(명령+결과) / 신뢰도 🟢🟡🔴 / 미해결 사항.
  검증을 안 돌렸으면 "미실행"이라고 **명시**한다. 🔴 상태에서 "완료됐습니다"라고 하지 않는다.
- **`git push` 는 매번 사람의 명시 승인 후.** 문서·설정 전용 PR(코드 변경 0)만 자동 머지 가능하고,
  **코드 변경이 포함된 PR 은 머지 전 반드시 사람 확인.**
- **비밀키는 서버 `infra/.env.production` 에만.** 코드·문서·설정 파일·채팅에 키 값을 남기지 않는다
  (이름·길이·ID 앞자리까지만 읽고 적는다).
- ⛔ **`BILLING_ENC_KEY` 변경·재생성 금지** / ⛔ **KPI·마케팅 문구에 '낙찰률' 사용 금지.**
- 에스컬레이션: 🟢 조회·되돌리기 쉬운 편집 / 🟡 배포·외부 발송·과금·대량 삭제 / 🔴 비밀키 변경·결제 실행.

커밋 시 로컬 git `pre-commit` 훅이 관련 검증을 다시 실행하고, 실패하면 커밋을 차단한다.
