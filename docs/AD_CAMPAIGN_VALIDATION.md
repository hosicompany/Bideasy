# 네이버 검색광고 14일 고객 검증 캠페인

> 상태: **제품·계측 게이트 완료 — 네이버 광고 계정 설정 진행** (2026-08-11)
> 목적: 매출 확대가 아니라 비치헤드와 훅을 판별한다. 안전 훅과 자격 훅 중 어떤 문제가 실제 행동으로 이어지는지 검증한다.
> 예산: 총 140,000원, 14일간 일예산 10,000원. 2026-08-11 사용자 집행 승인.

## 1. 작업 경계와 실행 좌표

- Git 저장소: `bideasy-suite/Bideasy`
- 전용 worktree: `bideasy-suite/Bideasy-customer-validation`
- 브랜치: `feat/customer-validation-202608`
- 기준점: `origin/master` `0d74137` (#101·#102 반영)
- 선행 의존성: PR #101 활성화 계측과 후속 PR #102 실사용 경로 보완·관리자 화면이 운영 배포됐다.

이 브랜치는 광고 랜딩, UTM, 측정, 실행 문서만 다룬다. 네이버 광고 계정, 결제수단, 충전,
캠페인 활성화와 입찰가·일예산 변경은 Git 바깥의 외부 작업이다. 2026-08-11 사용자 승인 범위에서
초기 설정을 진행하되 이후 예산 확대는 다시 확인한다. 비밀값과 광고 계정 식별자는 코드나 문서에
남기지 않는다.

## 2. 검증 대상과 성공 단위

- 비치헤드: 전문건설 1~10인 업체, 전기공사 우선, 월 10건 이상 직접 투찰하는 담당자.
- 가설 A — 안전: 투찰 전 금액과 기준선을 확인해 위험을 줄이려는 수요가 행동을 만든다.
- 가설 B — 자격: 업종·지역 조건에 맞는 공고를 먼저 좁히려는 수요가 행동을 만든다.
- 유효 고객 리드: 목표 업종의 실제 입찰 담당자이며 연락처 확인까지 완료한 사람.
- 활성 사용자: 계산 또는 진단을 완료하고 프로필 완성이나 첫 안전 판정까지 수행한 사람.

1차 지표는 유효 고객 리드 수와 CPA, 2차 지표는 활성 사용자 수다. 클릭률은 소재 진단용 보조
지표이며 성공 판정에 단독으로 사용하지 않는다. 유료 전환은 나오면 강한 신호지만 2주 실험의
필수 조건으로 두지 않는다.

## 3. 캠페인 구조

하나의 광고그룹에서 소재만 바꾸지 않고 훅별 광고그룹과 랜딩을 분리한다. 같은 키워드를 두 그룹에
중복 등록해 귀속을 흐리지 않는다.

⚠️ **훅 구분자는 `utm_campaign` 에 넣는다 — `utm_content` 가 아니다.** 2026-08-11 실측:
`utm_content` 는 프론트(`assets/app.js` 의 first-touch 저장은 source·medium·campaign 만 읽는다)
에도, 가입 스키마(`signup_*`)에도, `leads.utm_*` 컬럼에도 **저장되는 곳이 없다.** 두 훅의
source·medium·campaign 이 같으면 §7 이 1차로 쓰겠다고 한 DB 기록만으로는 훅을 가를 수 없고,
7일차 판정(한쪽 가치 행동 2배 → 예산 70% 배정)이 성립하지 않는다. 캠페인명을 훅별로 나누면
기존 `users.signup_campaign`·`leads.utm_campaign` 으로 그대로 갈린다(코드 변경 0).

### A. 안전 훅

- 키워드 후보: `투찰가 계산`, `투찰가 계산기`, `투찰가 산정`, `낙찰하한율 계산`,
  `낙찰하한가 계산`, `A값 계산`, `예정가격 계산`, `투찰률 계산`
- 제목: `투찰 전 위험 확인`
- 설명: `투찰가와 기준선을 계산하고 위험 구간을 확인하세요.`
- 랜딩:
  `https://bideasy.kr/calculator?utm_source=naver&utm_medium=cpc&utm_campaign=validation_202608_safety&utm_content=safety`

### B. 자격 훅

- 키워드 후보: `입찰자격 확인`, `공사 입찰자격`, `적격심사 계산`, `적격심사 계산기`
- 제목: `입찰자격 빠른 확인`
- 설명: `업종·지역 조건으로 확인할 공고를 좁혀보세요. 가입 없이 바로 진단해요.`
- 랜딩:
  `https://bideasy.kr/diagnose?utm_source=naver&utm_medium=cpc&utm_campaign=validation_202608_filter&utm_content=filter`

기존 소재의 `실적으로 넣을 수 있는 공고만` 표현은 현재 진단 범위를 넘으므로 사용하지 않는다.
안전 소재도 결과를 보장하는 표현 대신 계산과 위험 확인이라는 실제 기능만 약속한다.

## 4. 광고 설정

- 네이버 파워링크, 전국, PC+모바일.
- 수동입찰 시작가 150원, 키워드별 상한 300원.
- 일예산 10,000원, 총예산 140,000원.
- 시작 직전 네이버 키워드 도구로 PC·모바일 검색량과 예상 실적을 다시 확인한다.
- 2026-07-05 SERP 정찰 결과는 현재 경쟁 상태의 근거로 재사용하지 않는다.
- 검색 의도가 다른 검색어는 제외 키워드로 추가하되, 핵심 소재와 랜딩은 중간에 바꾸지 않는다.

## 5. 집행 전 게이트

- [x] PR #99 랜딩 신뢰 문구 정정·배포·라이브 확인.
- [x] PR #100 A값·Pro/Pro+ 설명 정합화·배포·라이브 확인.
- [x] PR #101 `profile_complete`·`activation_first_safe_check` 계측 병합·배포.
- [x] PR #102 실사용 계산·AI·가입 경로 계측 보완과 관리자 활성화 화면 병합·배포.
- [x] 운영 SHA `0d74137`, 마이그레이션 `6c034544c26d`, `/admin/stats/activation` 인증 가드 확인.
- [x] 두 UTM 랜딩 HTTP 200과 query string 유지 확인.
- [x] 두 랜딩의 **PC** 실제 화면 동작 확인(2026-08-11) — 계산기는 안전선 통과 판정·낙찰하한선
  89.745%·A값 미적용 표기 정상, 진단은 `POST /leads/diagnose` 실호출로 전기공사·부산 50건 매칭
  확인. `assets/app.js` 의 first-touch 저장이 두 랜딩 모두에서 동작(계산기도 이 스크립트를 읽는다).
- [ ] 두 랜딩의 **모바일** 실제 화면 확인 — 자동화 도구의 창 리사이즈가 스크린샷에 반영되지 않아
  미확인. viewport 메타·`theme.css` 미디어쿼리 8개·반응형 레이아웃은 정적으로 확인했으나
  실제 렌더링은 사람이 폰으로 열어봐야 한다.
- [x] 실험 표본은 캠페인 시작시각 이후 `utm_campaign` 이 `validation_202608_` 로 시작하는 유입만
  포함한다(`_safety`·`_filter` 두 값). 훅 판별도 이 컬럼으로 한다.
  기존 사용자 2명과 `e2e_check` 리드 1명은 기준선에서 제외한다.
- [ ] 네이버 키워드 도구 재확인 후 최종 키워드와 제외 키워드 확정.
- [x] 광고비 집행 승인(2026-08-11 사용자 지시 `미해결사항 진행`).

게이트가 닫히기 전에는 광고를 활성화하지 않는다. 추적 결함 수정 외의 신규 기능은 14일 동안 동결한다.

## 6. 14일 운영

| 기간 | 예산 | 운영 |
|---|---:|---|
| 1~3일 | 30,000원 | 두 훅 50:50. 노출·클릭·UTM·가치 행동의 연결을 확인한다. |
| 4~10일 | 70,000원 | 관련 없는 검색어를 제외한다. 7일차 판정 전 핵심 소재와 랜딩은 유지한다. |
| 11~14일 | 40,000원 | 승자가 있으면 70:30, 없으면 50:50을 유지한다. |

광고와 병행해 소개 또는 게시가 허용된 업종 커뮤니티에서 목표 고객 10~20명에게 접근하고,
실제 공고 한 건을 사용하는 15분 인터뷰 5회를 진행한다. 무단 대량 이메일·문자와 홍보성 게시물
도배는 하지 않는다. 교육형 콘텐츠는 `기초금액·추정가격·A값 구분`과 `참여 조건 확인 체크리스트`
두 편만 발행하고 각각 UTM을 구분한다.

## 7. 측정 퍼널과 일일 기록

퍼널은 `랜딩 → 계산/진단 완료 → 연락처 확인 → 가입 → 프로필 완성 → 첫 안전 판정 → 체험/결제`다.
GA4는 행동 흐름 확인에 사용하고, 가입·프로필·활성화·결제 판정은 DB 기록을 우선한다.

**훅 판별 = `utm_campaign` 접미사**(`_safety`/`_filter`). 조회 대상 컬럼은 가입이
`users.signup_campaign`, 리드가 `leads.utm_campaign` 이다. `utm_content` 는 GA4 에서만 보이고
DB 에는 남지 않으므로 판정 근거로 쓰지 않는다(§3 경고).

| 날짜 | 훅 | 비용 | 노출 | 클릭 | 계산/진단 완료 | 확인 리드 | 프로필 | 활성 | 인터뷰 | 결제 | 메모 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| YYYY-MM-DD | safety/filter | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | |

직접 소개는 `utm_source=founder_intro&utm_medium=referral`, 허가된 커뮤니티는 실제 채널명을
`utm_source`에 쓰고 `utm_medium=community`로 구분한다. 훅 구분이 필요하면 광고와 같은 규칙으로
`utm_campaign=validation_202608_safety` 또는 `_filter` 를 쓴다(`utm_content` 는 보조 표기).

## 8. 사전 등록 판정 기준

- 3일차: 10클릭 이상인데 가치 행동이 0이면 추적과 랜딩을 먼저 점검한다. 노출이 없으면 키워드
  검색량과 입찰가를 점검하며 훅 실패로 단정하지 않는다.
- 7일차: 훅당 약 30클릭 이상이고 한쪽의 가치 행동 또는 유효 고객 전환이 2배 이상이며 실제 전환이
  최소 3건이면 남은 예산의 70%를 배정한다. 조건 미달이면 50:50을 유지한다.
- 14일차 계속: 유효 고객 리드 7명 이상, 활성 사용자 3명 이상, CPA 20,000원 이하이며 인터뷰 3건
  이상에서 같은 문제가 반복된다.
- 제품 흐름 개선: 유효 고객 리드 5명 이상이지만 활성 사용자가 2명 미만이다.
- 유료 중단: 추적이 정상이고 100클릭 이상인데 유효 고객 리드가 없거나, 충분한 표본에서 CPA가
  20,000원을 초과한다.
- 비치헤드·메시지 재설계: 유료 검색과 허가된 직접 접촉 양쪽에서 목표 고객 반응이 없다.

## 9. 종료 후 결정

- 안전 훅 우세: 계산기 진입과 위험 확인 콘텐츠에 다음 4주 자원을 집중한다.
- 자격 훅 우세: 진단·프로필 onboarding과 익스텐션 CTA 연결을 강화한다.
- 리드는 있으나 활성화가 없음: 유입 확대를 멈추고 onboarding을 고친다.
- 리드가 없음: 기능 추가가 아니라 고객군·채널·메시지를 다시 정한다.

광고 계정의 실제 설정값과 일일 결과는 이 문서의 표 또는 별도 비밀값 없는 CSV로 남긴다. 광고비
집행, 외부 발송, 캠페인 활성화는 매번 전결 규칙에 따른 승인을 받는다.
