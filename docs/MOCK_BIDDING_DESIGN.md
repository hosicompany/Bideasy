# 모의투찰 (Shadow Bidding) — 설계 정본

> 작성 2026-08-02 · 갱신 2026-08-10 · 상태: **§0 사전 등록 기준 동결 유지 / Phase 1~3 구현 완료 / 운영 배포 대기**
> 목적: 실제 공고에 **개찰 전** 우리 알고리즘의 투찰가를 확정·저장하고, 개찰 후 자동 채점해 성능·개선점을 축적한다.
> 관련: `docs/BENCHMARK_WIN_REACH.md`(과거 데이터 벤치마크·게이트 전례) · `docs/COMPETITIVE_STRATEGY.md`(전략 정본) · `app/services/autocalibrate/`(백테스트·자가보정)

---

## 0. 사전 등록 (Pre-registration) — 지표·게이트 동결

**이 섹션은 구현 착수 전에 작성·동결되었다. 결과를 보고 지표나 게이트를 바꾸는 것(체리피킹)을 금지한다.**
`BENCHMARK_WIN_REACH.md` §0 과 같은 규율이다. 그때 이 규율이 있었기에 G1 의 결론을 정직하게 기각할 수 있었다.

### 0.1 이 기능이 백테스트와 다른 단 하나

이미 백테스트(`autocalibrate/optimizer`)도, 일일 사후 검증(`prediction_verifier`)도 있다. 그럼에도 이걸 만드는 이유는 **한 가지**다.

| | 백테스트 / 현 사후 검증 | 모의투찰 |
|---|---|---|
| 가격 결정 시점 | **개찰 후** (검증 시점의 active 전략으로 계산) | **마감 전** |
| 나중에 값이 바뀌나 | **바뀐다** — 자가보정이 매주 파라미터를 갱신하면 "그때 우리가 추천한 값"이 소급 변경됨 | **불변** (append-only) |
| 측정의 성격 | 사후 재구성 | 전향적 실험 |

`prediction_verifier.compute_recommendations(notice)` 는 개찰 후에 호출되고 `_get_active_strategy()` 를 읽는다. 즉 **현재 성능 측정은 자기충족적**이다. 마감 전에 가격을 못 박아야만 "우리 알고리즘이 실제로 얼마나 맞나"를 말할 자격이 생긴다.

### 0.2 지표 정의 (실행 전 동결)

판정 공식은 운영 중인 `optimizer.simulate_params`(optimizer.py:94-98)와 **동일**하다. 모의투찰이 자가보정과 다른 정의를 쓰면 두 시스템이 서로 다른 말을 하게 된다.

- **실제 하한선** = `reserved_price × lower_limit_rate / 100`
  - `lower_limit_rate` 는 **공고 명시값(`Notice.lower_limit_rate`) 우선**, 결측 시 `lower_limits.get_lower_limit_rate(contract_type, basic_price, bid_date)` 폴백. (2026-08-02 크롤러 수복으로 공고가 실측값을 주게 됐다 — 금액대 테이블 추정보다 정확하다.)
- **WIN** = 등록가 ∈ [실제 하한선, 실제 낙찰가]
- **DROPOUT (무효)** = 등록가 < 실제 하한선
- **LOST** = 하한선은 통과했으나 낙찰가 초과
- **NO_RESULT** = 개찰결과 미도착 (채점 보류 — 실패가 아니다)
- **VOID** = 취소공고·유찰 등 판정 대상 아님

**지표 우선순위 — 이 순서를 뒤집지 않는다.**

| 순위 | 지표 | 왜 이 순위인가 |
|---|---|---|
| **1차** | **무효율(dropout)** | 브랜드 KPI 가 유효율이다(전역 규칙 §4-2). 무효는 입찰 자체가 없던 일이 된다 |
| 2차 | 적중률(win) | 자가보정과 동일 정의 |
| 3차 | **실제 등수 분포** | 참가자 데이터가 있어야 산출(§4-3). 승패 이진값으로는 개선 방향이 안 보인다 |
| 4차 | **사정률 예측 오차** `\|reserved/basic − 예측\|` | 개별 승패는 추첨 노이즈가 크지만, 이 오차는 우리 모델의 순수 신호다 |

세그먼트: `bid_method × 금액대(bracket) × 기관 × 지역`.

### 0.3 비교 대상 5 arm (동시 등록)

실제 돈이 나가지 않으므로 arm 을 여러 개 낼 비용이 0 이다. 하나만 기록하면 "다른 선택을 했다면?"을 영영 잃는다.

| arm | 정의 | 근거 |
|---|---|---|
| `standard` | 기초금액 × 97.5% (사정률 −2.5) | 사용자 슬라이더 기본값 — 실사용자 다수의 실제 행동. 과거 win 0.52% |
| `active` | 현 자가보정 active 전략 | 운영 정본 |
| `frontier_c5` | dropout 캡 5% 최적 파라미터 | 벤치마크 §4 "무릎" 하단 |
| `frontier_c10` | dropout 캡 10% 최적 파라미터 | holdout 2025 win 32.1% |
| `aggressive` | 기초금액 × 88.0% (−12) | 경쟁사 추격 가설. 과거 무효 27.6% |

### 0.4 게이트 (실행 전 동결)

**G-A · 파이프라인 건전성** (착수 후 첫 4주)
- 조건: 등록분의 **채점 도달률 ≥ 60%** (등록했는데 개찰결과가 안 붙으면 데이터가 안 쌓인다)
- 미달 시: 지표 해석 금지. 원인(조인 실패·개찰 지연·취소공고)부터 수습한다.

**G-B · 전략 우열 판정** (적격심사제 채점 누적 **≥ 400건** 도달 시)
- 조건: `active` 의 무효율이 `standard` 이하이면서, win 의 Wilson 95% CI **하한**이 `standard` 의 CI 상한을 상회
- 통과 시: "우리 추천이 기본값보다 낫다"를 우리 데이터로 말할 수 있다(대외 표기는 §0.6 준수).
- 400건 기준은 `BENCHMARK_WIN_REACH.md` §5-3 의 G2 재판정 조건과 같은 값 — 그 재판정을 이 데이터로 수행한다.

**G-C · 전략투찰 제품화 판정** (G-B 통과 후)
- 조건: `frontier_c10` 의 win CI 하한이 `active` 의 CI 상한을 상회 **그리고** 무효율 ≤ 11%
- 통과 시: Pro+ "전략 투찰(고위험 고지)" 모드 제품화 **검토** 착수 (제품화 자체는 별도 결정).
- 불통과 시: 현 포지션 유지. 이건 실패가 아니라 "안전 우선이 옳았다"는 답이다.

**킬 기준 (사전 등록 — 사후 합리화 금지)**
- 착수 8주 후에도 G-A 미달이면 **기능을 중단**하고 원인 분석만 남긴다.
- 등록·채점이 도는데 6개월간 어떤 arm 도 `standard` 를 유의하게 이기지 못하면, "알고리즘 우위 주장"을 접고 제품 서사를 안전·유효율로만 좁힌다.

### 0.5 유효성 체크 (구현에 내장, 실패 시 등록 거부)

1. `registered_at < deadline_at` — **위반 시 등록 거부**. 이 가드가 실험 전체의 신뢰 근거다.
2. `(bid_no, arm)` UNIQUE — 재실행이 중복 등록을 만들지 않는다.
3. 채점 시 `mock_bids` 행은 **절대 UPDATE 하지 않는다**. 재채점은 `mock_bid_results` 에 새 행(`scoring_rev`+1).
4. arm `active` 의 등록가는 같은 파라미터로 `recommend_bid_price` 를 다시 부른 값과 일치해야 한다(구현 자체 검증).

### 0.6 대외 표기 금지 사항

- ⛔ **"낙찰률 N%" 마케팅 금지** (전역 규칙 §4-2). 모의투찰 결과를 낙찰률로 홍보하면 규칙 위반이다. 대외 지표는 **유효율(무효 0)** 과 재사용률.
- ⛔ 이건 **counterfactual(반사실) 재구성**이다. "우리가 실제로 넣었어도 같은 결과"라는 보장이 아니다(§10).
- ⛔ 표본이 게이트 미달인 상태의 수치를 근거로 쓰지 않는다.

---

## 1. 배경 — Phase 0 정찰 실측 (2026-08-02, 프로덕션)

| 항목 | 실측 | 판정 |
|---|---|---|
| 개찰결과 누적 | 4,511건 (2026-05-18~07-31) | 기반 있음 |
| 개찰 유효율(기초·낙찰·예정가 모두 존재) | **99.2%** | 채점 가능 |
| `reserved_price` 보유 | 99.5% | 하한선 정확 계산 가능 |
| bid_no 조인 포맷 | 양쪽 `-000` 3자리 | 포맷 문제 없음 |
| 하루 개찰 | 140~320건 | |
| 공사 공고 마감 | 하루 300~430건 | 등록 대상 충분 |
| 적격심사제 비중(2026 **개찰**) | **7.2%** (323/4,473) | 단 진행중 공고 기준은 다르다(아래) |
| `participants_count` | **0%** (전건 NULL) | §4-3 이 필요한 이유 |
| `Notice.a_value` | **0.09%** (3/3,511) | §10 한계 1 |

### 1-1. 백필 후 재측정 (2026-08-02, PR #57 배포·백필 완료 직후)

| 항목 | 백필 전 | 백필 후 | 의미 |
|---|---|---|---|
| 공사 공고 수 | 3,521 | **35,503** | 일일 크롤이 최근 5일·5페이지만 봐서 대량 누락하고 있었다 |
| `bid_method` 채움 | 0 (0%) | **35,497 (99.98%)** | |
| **조인 성공** (notices↔opening_results) | 990 | **4,499** | |
| **개찰결과 중 우리 공고 보유율** | 22% | **99.7%** | Phase 0 미해결이던 "조인율 22%"의 정체 = 공고 수집 범위 부족. 해소됨 |
| 하한율 공고 명시 커버리지(진행중) | — | **92.6%** | 폴백 필요분은 7.4%뿐 |

**등록 대상 규모 (§3 규칙 적용, 진행중 공고 기준)**: **1,800건**
- 소액수의견적 920 · **적격심사제 733** · 제한적최저가 79 · 최저가낙찰제 68
- 향후 7일 마감: 하루 151~397건

⚠️ **개찰 기준 적격심사제 비중(7.2%)과 진행중 기준(40.7%)이 크게 다르다.** 소액수의견적은 공고~개찰 주기가 짧아 회전이 빠르고 적격심사제는 공고 기간이 길어, "현재 진행중" 스냅샷에는 적격이 훨씬 많이 잡히는 것으로 보인다. **모의투찰은 진행중 공고에 등록하므로 적격심사제 표본이 개찰 분포보다 빠르게 쌓인다**(§10-4 정정).

**기타 실측**
- `notice_kind`: 등록공고 31,249 · **취소공고 1,977 (5.6%)** · 변경공고 1,971 (5.6%) · 재공고 300 → §3 제외 규칙과 §6 태깅의 실측 근거.
- 예가 구성: **15/4 가 88.8%** (31,316건), **1/0 이 11.2%** (3,933건, 복수예가 미사용). `simulation_service` 의 15/4 하드코딩은 약 89% 에만 맞다.
- A값: 35,503건 중 **3건**. 분모가 10배 늘었는데 분자는 그대로 — A값 3-tier 파이프라인이 실질 미작동임이 더 분명해졌다(§10-1).

**수습된 선행 결함** (PR #57, 2026-08-02 배포):
- 공고 크롤러가 존재하지 않는 API 키를 읽어 `bid_method`·`region` 등이 **100% 결측**이었다 → `recommend_bid_price` 가 전 건 `DEFAULT` 전략으로 떨어지고 있었다. 이 상태로 모의투찰을 붙였다면 **측정하려는 전략이 아닌 것을 측정**했을 것이다.
- 백필 후 공사 `bid_method` 채움률 0% → **99.8%**.
- 부수 소득: `Notice.lower_limit_rate`(공고 명시 하한율)·`prdprc_total/draw`·`notice_kind` 확보.

---

## 2. 설계 원칙

**P1. 사전 등록이 전부다** — 마감 전 확정, 이후 불변. 전략 버전·코드 리비전과 함께 **그 시점에 우리가 본 입력 스냅샷**을 박는다. 기초금액이 정정(`notice_kind='변경공고'` 실재)되거나 A값이 나중에 백필되면 "그때 본 정보"가 달라지는데, 스냅샷이 없으면 사후 변명이 가능해진다.

**P2. 한 공고에 5 arm 동시 등록** — §0.3.

**P3. 판정 함수는 단일 소스** — `simulate_params` 와 같은 정의를 공유하는 함수 하나로 뽑는다.

**P4. 참가자 데이터는 등록한 공고만 저장** — 공사 개찰은 하루 169k행 규모(census 조사)라 전수 저장은 함정이다. 등록분으로 한정하면 데이터량이 수십분의 1로 떨어지면서 얻을 건 다 얻는다.

**P5. 초기엔 자가보정 루프에 자동 연결하지 않는다** — 설계가 덜 검증된 실험이 운영 전략을 바꾸면 안 된다. G-B 통과 후 수동 승격.

---

## 3. 등록 대상 선정 규칙 (사전 고정 — 편향 방지)

"좋아 보이는 공고만" 등록하면 지표가 부풀려진다. 규칙을 고정하고, 예외를 두지 않는다.

**포함**: `contract_type='CONSTRUCTION'` · `basic_price > 0` · `end_date` 가 미래 · `bid_method` 가 비어있지 않음

**제외**:
- `notice_kind = '취소공고'` — 실재한다(정찰에서 발견). 취소된 공고에 투찰할 수는 없다.
- `bid_method` 가 전략 미정의(`협상에의한계약` 등 가격경쟁이 아닌 방식)
- 이미 `(bid_no, arm)` 등록된 건

⚠️ 제외 규칙을 나중에 추가하면 그 시점 이전/이후 데이터가 섞인다. 바꿔야 한다면 **버전을 올리고 분석에서 분리**한다.

---

## 4. 데이터 모델

### 4-1. `mock_bids` — 사전 등록 (불변)

```
id, bid_no, arm                       -- (bid_no, arm) UNIQUE
registered_at, deadline_at            -- registered_at < deadline_at 강제
strategy_version, code_rev            -- 재현성
price, bid_rate, adjustment, margin
snapshot_basic_price                  -- ★ 입력 스냅샷 (아래 전부)
snapshot_a_value, a_value_source      -- tier1 | tier2 | none
snapshot_lower_limit_rate, llr_source -- notice | table  (어느 쪽을 썼는지)
snapshot_bid_method, snapshot_contract_type, snapshot_notice_kind
status                                -- REGISTERED | SCORED | VOID
```

### 4-2. `mock_bid_results` — 채점 (재채점 시 새 행)

```
id, mock_bid_id, scoring_rev
outcome                               -- WIN | LOST | DROPOUT | NO_RESULT | VOID
actual_reserved_price, actual_winner_price, actual_lower_limit
estimated_rank, participants_count    -- ★ 4-3 이 있어야 채워진다
gap_to_winner_pct, gap_to_limit_pct
reserved_ratio_actual, reserved_ratio_predicted, ratio_error
failure_tags                          -- ★ JSON 배열 (§6)
scored_at
```

### 4-3. `opening_participants` — 정답지 확장

```
bid_no, rank, company, bid_price, bid_rate, sucsf_yn
```

지금 `opening_result_crawler._parse_item_to_kwargs` 는 참가자 행을 `return None` 으로 **버린다**(낙찰자만 저장). API 는 전원의 `bidprcAmt`·`opengRank` 를 주고 있다. 이걸 주우면 판정이 이렇게 달라진다:

| 지금 | 참가자 저장 후 |
|---|---|
| WIN/LOST/DROPOUT 3분류 | **실제 몇 등이었는지** |
| "졌다" | "**2위, 낙찰가와 0.003%p 차이**" |
| 경쟁 강도 모름 | 참여자 수 실측 → 참여자 예측 모델 검증 |

---

## 5. 파이프라인 (기존 beat 에 3개 추가)

```
06:00  notices.crawl_daily                       (기존)
06:30  notices.backfill_avalue                   (기존)
매시   mock_bid.register          ★신규 — 마감 2h 전 이내 & 미등록 공고를 arm 별 등록
19:00  verification.daily_crawl_opening_results  (기존 · 참가자 저장으로 확장)
20:30  mock_bid.score             ★신규 — 개찰결과 도착분 채점
月 21:00 mock_bid.weekly_report   ★신규 — 게이트·표본품질·큐·오답노트 성적표
```

**왜 매시인가**: 마감시각이 공고마다 다르다. 하루 1회로는 "마감 전 등록"을 보장할 수 없다. 등록 태스크는 `registered_at < deadline_at` 을 코드로 강제하고, 위반 시 그 건을 **거부**한다(§0.5-1).

---

## 6. 오답노트 자동 태깅 — "신경써야 할 요소" 발견 장치

실패 건마다 사유 태그를 붙이고 태그별 실패율을 집계한다. 여기서 나온 사실이 곧 제품 기능(경고 배지)이자 콘텐츠(데이터스토리)가 된다.

`A값_결측` · `A값_추정치` · `기초금액_정정(변경공고)` · `사정률_극단(상하위5%)` · `동가밀집` · `참여자_폭증` · `재공고건` · `예가_비표준(prdprc≠15/4)` · `하한율_테이블폴백` · `개찰지연`

예: *"A값이 없는 공고에서 무효율이 3배"* 가 나오면, 그건 그대로 사용자 경고가 된다.

---

## 7. 함정·금지 (구현 시 준수)

1. **`mock_bids` 를 UPDATE 하지 말 것** — 사전 등록의 의미가 사라진다. 재채점은 결과 테이블에 새 행.
2. **낙찰률 대외 표기 금지** — §0.6.
3. **등록 대상 규칙을 조용히 바꾸지 말 것** — §3.
4. **판정 정의를 `simulate_params` 와 갈라놓지 말 것** — 갈라지면 자가보정과 모의투찰이 다른 말을 한다.
5. **참가자 전수 저장 금지** — 등록분만(§P4).
6. **자가보정 자동 연결 금지** — G-B 통과 전까지(§P5).
7. **취소공고를 등록하지 말 것** — `notice_kind` 로 걸러진다.
8. **등수는 유효 투찰만 센다** — 개찰 API 는 낙찰하한선 이상에만 `opengRank` 를 준다
   (운영 표본의 47.6% 가 무효 = 순위 없음). 무효까지 세면 개찰조서와 다른 축이 되고,
   실제로 등수가 평균 15.79 부풀어 있었다(§9 · 2026-08-11). `estimate_rank` 에는 반드시
   `_valid_participant_prices` 로 거른 목록만 넘긴다. `sucsf_yn` 으로 거르지 말 것 —
   그건 '낙찰자인가'라 1명만 남는다. 축이 다시 갈라지는지는 `rank_axis_health` 가 감시한다.

---

## 8. 단계 계획

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 정찰 — 조인율·데이터 품질·필드 실측 | ✅ 완료 (2026-08-02) |
| 0.5 | 크롤러 필드 수복 + 백필 | ✅ 완료 (PR #57) |
| **1** | 테이블 3종 + 등록·채점 태스크 + 어드민 조회 | ✅ 완료·운영 중 |
| 2 | 참가자 저장 + 실제 등수 재구성 | ✅ 완료·운영 중 |
| 3 | 오답노트 태깅 + 주간 성적표 | ✅ 구현 완료 (주간 태스크는 2026-08-10 로컬, 배포 대기) |
| 4 | 자가보정 승격 게이트 (G-B 통과 조건부) | ✅ 자동 판정 구현 / ⏸ 승격은 G-B 통과 전 금지 |

---

## 9. 결과 (구현·운영 후 채움)

### 2026-08-10 운영 재실측 (수정 배포 전)

| 항목 | 값 | 판정 |
|---|---:|---|
| 등록 | 8,865 arm 행 = **1,773공고** | arm 5개를 표본 5개로 세지 않는다 |
| 확정 채점 | 2,480 arm 행 = **496공고** | 전체 G-A **27.98%** |
| 마감 도래 코호트 | 1,231공고 중 354공고 확정 | 도달률 **28.72%** |
| 유효 성능 표본 | 496공고 중 **59공고** | 437공고는 기초금액/예정가격 기준 불일치 |
| 적격심사제 | 원시 확정 28공고 / 유효 **2공고** | G-B 최소 400공고 미달 |

**G-A = OBSERVING·해석 차단. G-B = BLOCKED_G_A. G-C = LOCKED_G_B.**

종전 문서의 “SCORED 2,275건이므로 G2 400건 충족”은 `mock_bids`의 **arm 행**을
distinct 공고 표본으로 오독했고, 기초금액 기준 불일치도 제거하지 않은 결론이라
폐기한다. 2026 G2도 일관성 통과 holdout이 약 59건으로 아직 `NOT_READY`다.

### 원인과 2026-08-10 로컬 수정 (미배포)

- 오래된 NO_RESULT 991공고가 4,955 arm 행을 차지해 일일 limit 5,000을 거의
  독점했다. “다음 회차가 밀린 잔량을 처리한다”는 가정은 틀렸다.
- 채점 우선순위를 **개찰결과 보유 → 한 번도 확인하지 않음 → NO_RESULT 재시도**로
  고정하고, 세 큐의 arm 행/공고 수와 배치 전후 잔량을 노출한다.
- 모든 성능 집계는 `예정가격 / 등록시점 기초금액 = 0.94~1.06`인 최신 rev만
  사용하고 제외 수를 함께 보인다. 원장 행은 삭제·수정하지 않는다.
- G-A/G-B/G-C는 distinct 공고, 적격심사제 유효 표본, Wilson 95% CI로 자동
  판정한다. G-A 또는 유효 표본이 없으면 관리자 성능 화면을 잠근다.
- 월요일 21:00 누적 성적표와 2026 G2 재실행 경로를 추가했다. 자가보정 전략의
  자동 승격은 여전히 금지다(P5).
- 주간 성적표 생성 실패는 성공 결과로 삼키지 않는다. Celery가 최대 3회 재시도한
  뒤에도 실패하면 태스크를 `FAILURE`로 남겨 다음 주까지 조용히 누락되지 않게 한다.
- 2026 G2의 DB 조회 실패는 빈 표본 `NOT_READY`가 아니라 실행 실패다. 비정본 결과는
  source·method별 파일로 분리해 서로 덮어쓰지 않는다.

### 2026-08-11 독립 리뷰 — 등수 축 전제가 틀렸다 (운영 실측)

PR #62(Phase 2) 를 독립 리뷰하다 **등수 지표가 개찰조서와 다른 물건을 재고 있었다**는 것이
운영 데이터 대조에서 드러났다. 코드는 이렇게 단정하고 있었다 —
*"`opengRank` 와 같은 축이며 하한선 미달 참가자도 순위에 포함된다."* **사실과 반대였다.**

| 실측 (참가자 462,900행 전수) | 값 |
|---|---:|
| `rank` 결측 = 무효 투찰 | 220,491행 (**47.6%**) |
| 무효 포함 재계산 vs 저장된 `opengRank` 불일치 | **97.8%** |
| **유효 투찰만** 재계산 vs `opengRank` 불일치 | **0.054%** (130/242,409) |
| 등수가 채워진 채점 건 | 3,435 |
| **그중 무효(DROPOUT)** | **2,460 (71.6%)** — 아래 참조 |
| 유효 판정분(WIN+LOST) 평균 등수 → 실제 | 103.58 → **56.17** |
| 유효 판정분 1위 표시 vs 실제 1위 | 37 → **71** |

> ⚠️ **초판 정정**: 이 표의 첫 판본은 *"평균 32.47 → 16.68, 1위 2,159 → 2,524"* 였는데,
> 그 숫자는 아래 DROPOUT 인공물을 포함한 값이라 폐기한다. 무효 건을 빼고 다시 낸 값이 위 표다.

**같은 수정에서 드러난 두 번째 결함 — 무효(DROPOUT)가 정의상 항상 "1위"였다.**
등수를 유효 투찰 기준으로 고치자, 유효 투찰은 전부 하한선 이상인데 DROPOUT 은 하한선
미만이라 `estimate_rank` 가 **언제나 1** 을 돌려주게 됐다. 실측: DROPOUT 2,460건 중
**2,453건(99.7%)** 이 "1위". 그대로 두면 등수 분포에서 **무효를 많이 내는 arm 일수록
성적이 좋아 보여** §0.2 1차 지표(무효율)와 정반대로 움직인다.
→ **무효는 등수를 갖지 않는다**(`score_mock_bid`·백필·`rank_distribution` 모두).
참가자에게 적용하는 규칙("무효 투찰에는 순위가 없다")을 우리 자신에게도 적용한 것이다.
기존 2,460건은 백필이 등수를 지운 새 rev 로 정정한다.

**`rank` 결측 = 무효 전제의 확증**: 결측 220,491행 중 같은 공고의 유효 최저가보다
비싼(= 무효로 설명되지 않는) 행은 **890건, 0.4%** 뿐이다.

**API 는 낙찰하한선 이상 유효 투찰자에게만 `opengRank` 를 준다.** 무효 투찰은 순위 자체가 없다.
`sucsf_yn` 은 '낙찰자인가'(적격검사 통과)라 무효 여부와 다른 축이다 — 그걸로 거르면 1명만 남는다.

**조치**: 등수·분모를 유효 투찰 기준으로 고정(`_valid_participant_prices`), 분모를
`valid_participants_count` 로 분리 저장(마이그 `a3f9d2c47e61` — 전 참가자 수는
경쟁 강도 지표로 그대로 둔다). 기존 3,435건은 `backfill_participant_ranks` 가
값이 달라진 건을 **새 scoring_rev** 로 다시 계산한다(원장 행 불변 — §0.5-3).

**재발 방지**: `rank_axis_health` 가 두 가지를 상시 잰다 — ① 순위 있는 행끼리 가격순이
맞는가(전수 기준선 0.054%) ② **순위 결측률**(기준선 47.6%). ②가 핵심이다. 이번 사고는
①로는 **절대 안 잡힌다** — 순위 있는 행끼리는 언제나 가격순이기 때문이다. 결측이
사라지면(API 가 무효에도 순위를 주기 시작) 또는 전면화하면(필드명 변경 등 파싱 실패)
전제가 깨진 것이다. 어긋나면 등수 차트와 **`가상 3위 안에 든 비율` KPI 를 함께** 잠근다
— 차트만 막고 숫자를 남기면 관리자가 경고를 읽고도 그 숫자를 보고서에 옮긴다.
**검증 재료를 같은 테이블에 갖고 있으면서 대조하지 않은 것이 이 사고의 본질이다** —
축이 어긋난 9일 동안 테스트 697건은 전부 통과했고 화면도 멀쩡히 그려졌다.

**남은 지적(미수정)**: ① `_save_participants` 가 행 수가 줄어드는 교체를 막지 않는다
(부분 응답이 완전 집합을 덮어쓰면 백필이 그 퇴행값을 새 rev 로 승격시킨다)
② 참가자 수집이 전부 실패해도 크롤이 `ok: True` 로 보고한다(정상 화면과 구분 불가)
③ `(mock_bid_id, scoring_rev)` UNIQUE 부재 — 실측 중복 0건이라 예방 조치
④ 참가자 dedupe 키가 결측 동가 2행을 병합 ⑤ `opening_participants` 보존기간 없음.

### 배포 후 복구 확인 순서

1. `queue_health.ready_with_opening_result_arm_rows`가 첫 채점 배치 뒤 0에 수렴하는지 본다.
2. `never_checked_arm_rows`가 줄고, `retry_no_result_arm_rows`만 남는지 본다.
3. G-A와 마감 도래 코호트 도달률을 1일·7일 간격으로 기록한다.
4. G-A 60% 전에는 성능 우열을 해석하지 않는다. 적격 유효 400공고 전에는 G-B를
   판정하지 않고, G-B 통과 전에는 G-C·자가보정 승격을 열지 않는다.

---

## 10. 한계 (사전 인지)

1. **A값 결측 0.09%** — 공사 투찰 공식의 핵심 변수인데 사실상 없다. 모의투찰은 A값 없이 계산할 수밖에 없고, 그건 **A값을 넣는 실사용자의 투찰과 체계적으로 다르다**. 다만 `recommend_bid_price` 와 벤치마크도 같은 조건이라 내부 비교의 일관성은 유지된다. 태깅(§6)으로 영향을 정량화한다.
2. **Counterfactual 가정** — "우리가 참여해도 시장(타 참가자 가격·예정가격)은 불변". 적격심사 최저가 순 심사에서는 근사 타당하나 완전하지 않다. 참여자가 적은 소액 건에서 특히 약하다.
3. **순위·동가추첨 미모델링** — WIN 은 "낙찰자보다 낮은 유효가"이지 1순위 보장이 아니다. Phase 2 의 참가자 데이터가 이 한계를 부분적으로 걷어낸다.
4. ~~**적격심사제 표본 증가 속도** — 진행중 비중 40.7%이므로 G-B(400건)는 수 주 규모다.~~
   → **2026-08-10 재정정**: 진행중 비중만으로 유효 표본 속도를 예측할 수 없다. 운영에서 적격 원시 확정은 28공고였고 기초금액 일관성 통과는 **2공고**뿐이며, G-A도 27.98%다. 병목은 대상 비중보다 **채점 도달과 금액 기준 품질**이다. 복구 후 실제 증가율로 다시 추정한다.
5. **공사 전용** — 물품은 하한선이 없어 안전 게임이 아니고, 용역은 `sucsfbidLwltRate` 미제공 건이 많다.
